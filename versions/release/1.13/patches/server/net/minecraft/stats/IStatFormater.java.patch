--- a/net/minecraft/stats/IStatFormater.java
+++ b/net/minecraft/stats/IStatFormater.java
@@ -35,4 +35,6 @@
          return d1 > 0.5D ? DECIMAL_FORMAT.format(d1) + " m" : d0 + " s";
       }
    };
+
+   String format(int p_format_1_); // MCP: add client-only method to server
 }
